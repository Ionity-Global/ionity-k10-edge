// IonityEdge · K10 (UNIHIKER core) — SECRETS TEMPLATE
// Copy this file to secrets.h and fill in real values. secrets.h is git-ignored.
// NEVER commit real credentials to a public repo. Prefer installer provisioning.
// If secrets.h is absent the firmware falls back to WIFI_PASS "" (provision over serial/NVS).
// © Ionity (Pty) Ltd · Policy 986 AED
#pragma once

#define WIFI_SSID   "Antwerp Ionity"
#define WIFI_PASS   "your-wifi-password-here"

// Optional overrides (else the build_flags / code defaults apply):
// #define EDGE_HOST  "192.168.124.5"
// #define EDGE_PORT  8765
